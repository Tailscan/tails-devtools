export enum actions {
  changeConfig = "changeConfig",
  complete = "complete",
  resolveCompleteItem = "resolveCompleteItem",
  resolveColor = "resolveColor",
  findColor = "findColor",
  hover = "hover",
  compile = "compile",
  compiled = "compiled",
}
